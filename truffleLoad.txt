dependenc:
npm install -g truffle // Version 3.0.5+ required.
npm install -g ethereumjs-testrpc
 


cd to truffle
cd to contracts / truffle compile
new cmd - testrpc
cd to migrations / truffle migrate
cd to root / npm run start
